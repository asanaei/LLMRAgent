% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_agent.R
\name{new_agent}
\alias{new_agent}
\title{Create Agent with Model Config}
\usage{
new_agent(system_prompt = "", model_config, memory = new_buffer_memory(10))
}
\arguments{
\item{system_prompt}{System message to prepend.}

\item{model_config}{LLMR model config from \code{LLMR::llm_config()}. Required.}

\item{memory}{Memory object from \code{new_buffer_memory()}.}
}
\value{
An agent environment with built-in usage tracking.
}
\description{
Simple agent that uses LLMR model configuration directly.
Automatically tracks token usage across all interactions.
Requires a valid LLMR model config to function.
}
\examples{
if (requireNamespace("LLMR", quietly = TRUE) && Sys.getenv("OPENAI_API_KEY") != "") {
  config <- LLMR::llm_config(provider = "openai", model = "gpt-4", 
                             api_key = Sys.getenv("OPENAI_API_KEY"))
  ag <- new_agent(system_prompt = "Be brief.", model_config = config)
  # agent_reply(ag, "Hello")
  # usage <- agent_usage(ag)  # Check token usage
}
}
\seealso{
\code{\link[=agent_reply]{agent_reply()}}, \code{\link[=agent_usage]{agent_usage()}}, \code{\link[=agent_usage_reset]{agent_usage_reset()}}
}
